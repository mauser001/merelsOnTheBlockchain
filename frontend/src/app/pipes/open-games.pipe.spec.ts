import { OpenGamesPipe } from './open-games.pipe';

describe('OpenGamesPipe', () => {
  it('create an instance', () => {
    const pipe = new OpenGamesPipe();
    expect(pipe).toBeTruthy();
  });
});
