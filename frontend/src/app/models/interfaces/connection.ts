export interface Connection {
    contractAddress: string
    chainId: number
    chainRpc: string
    chainName: string
    currencySymbol: string
    blockExplorerUrl: string
    costInEth: string
}
